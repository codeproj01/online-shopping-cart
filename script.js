
//placeholder: if statement for /component/modal /component/confirm dialog box
//see component/Confirm and /confirm scripts
if (confirm('Are you sure?')) {
    handleConfirmed();
  } else {
    handleAborted();
  }